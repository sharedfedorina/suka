# 🔧 Технічна реалізація конструктора лендінгів

## 📦 Архітектура

```
HTML/CSS/JS → JSON → EJS Template → Dynamic HTML
```

### Компоненти системи

1. **Express.js сервер** (server.js)
   - Маршрути: GET, POST
   - JSON middleware
   - Static файли (CSS, JS, зображення)

2. **EJS Шаблон** (template.ejs)
   - Динамічні цикли forEach
   - Вставлення змінних <%= %>
   - 1530 рядків HTML

3. **JSON Дані** (landing-data.json)
   - Структурована інформація
   - 5 товарів, 5 переваг
   - Всі метаданні та настройки

4. **Static файли** (public/)
   - CSS (Responsive, BEM)
   - JavaScript (Vanilla, Swiper)
   - Шрифти, зображення, іконки

---

## 🔄 Потік виконання

### GET /generate (дефолтні дані)

```
1. GET /generate
   ↓
2. Прочитати data/landing-data.json
   ↓
3. Передати дані в EJS
   ↓
4. res.render('template', { data: defaultData })
   ↓
5. EJS обробляє <%= data.hero.title %>, циклів forEach, тощо
   ↓
6. Повертає готовий HTML
   ↓
7. Браузер отримує HTML і отображає сайт
```

### POST /generate (користувацькі дані)

```
1. POST /generate + JSON body
   ↓
2. Прочитати JSON з request body
   ↓
3. Передати дані в EJS
   ↓
4. res.render('template', { data: customData })
   ↓
5. EJS обробляє шаблон з новими даними
   ↓
6. Повертає готовий HTML
   ↓
7. JavaScript у браузері відкриває HTML у новій вкладці
```

---

## 📝 EJS Шаблон - Приклади

### Вставлення змінних

**Простий текст:**
```ejs
<title><%= data.metadata.title %></title>
<h1><%= data.hero.title %></h1>
```

**Умовні конструкції:**
```ejs
<% if (data.hero.price) { %>
  <span><%= data.hero.price %> <%= data.hero.currency %></span>
<% } %>
```

### Цикли

**Переваги (5 елементів):**
```ejs
<% data.advantages.forEach((adv, index) => { %>
  <li class="plus-list_point">
    <img src="<%= adv.icon %>" alt="img">
    <span>
      <b><%= adv.title %></b> <br>
      <%= adv.description %>
    </span>
  </li>
<% }) %>
```

**Товари (5 елементів):**
```ejs
<% data.products.items.forEach((product, index) => { %>
  <div class="products-list_point">
    <h3><%= product.name %><br><%= product.subtitle %></h3>
    <div class="price">
      <span class="discount"><%= product.price_old %> <%= product.currency %></span>
      <span class="total"><%= product.price_new %> <%= product.currency %></span>
    </div>
    <% if (product.specs) { %>
      <ul>
        <% Object.entries(product.specs).forEach(([key, val]) => { %>
          <li><span><%= key %>:</span><span><%= val %></span></li>
        <% }) %>
      </ul>
    <% } %>
  </div>
<% }) %>
```

---

## 📊 Структура JSON

### Metadata
```json
"metadata": {
  "title": "Професійні 3D принтери для виробництва | Printex Pro",
  "description": "...",
  "brand": "Printex Pro",
  "lang": "uk"
}
```

### Hero секція
```json
"hero": {
  "title": "Професійні 3D принтери",
  "price": "8 500",
  "currency": "грн",
  "count": "12",
  "count_unit": "принтерів за акційною ціною",
  "image": "img/start/start-1.png",
  "image_mobile": "img/start/start-1_m.webp"
}
```

### Переваги (масив)
```json
"advantages": [
  {
    "id": "plus1",
    "title": "Безкоштовна доставка",
    "description": "Доставимо будь-куди по Україні...",
    "icon": "icons/plus/plus-1.svg"
  },
  // ... ще 4
]
```

### Товари (масив)
```json
"products": {
  "items": [
    {
      "id": "product1",
      "name": "3D Принтер FDM Pro Max",
      "subtitle": "Точність: 0.1мм",
      "price_old": "13 200",
      "price_new": "8 500",
      "currency": "грн",
      "discount_percent": "35",
      "images": {
        "desktop": "img/products/product_1.jpg",
        "mobile": "img/products/product_1_m.webp",
        "gallery": ["img/products/product_1.jpg", ...]
      },
      "specs": {
        "type": "FDM",
        "bed_size": "300x300x400мм",
        "materials": "PLA, PETG, ABS, TPU"
      },
      "description": "Професійний 3D принтер..."
    },
    // ... ще 4 товари
  ]
}
```

---

## 🎨 CSS архітектура

### BEM методологія

```css
/* Блок */
.products-list { ... }

/* Елемент блоку */
.products-list_point { ... }
.products-list_title { ... }

/* Модифікатор */
.products-list_point--disabled { ... }
.products-list_point--premium { ... }
```

### Responsive дизайн

```css
/* Desktop first */
@media (max-width: 1024px) { /* Tablet */ }
@media (max-width: 768px) { /* Mobile */ }
```

---

## ⚙️ JavaScript функціональність

### Swiper.js (слайдери)

```javascript
// Ініціалізація слайдера товарів
new Swiper('.products-slider', {
  direction: 'horizontal',
  loop: false,
  navigation: {
    nextEl: '.products-next',
    prevEl: '.products-prev',
  }
});
```

### Обробка подій

```javascript
// Приклад з main.js
document.addEventListener('click', function(e) {
  if (e.target.matches('[data-id]')) {
    // Обробка кліку на кнопку замовлення
    const productId = e.target.dataset.id;
    console.log('Замовити: ' + productId);
  }
});
```

---

## 🔄 Про динамічне оновлення

### Перезапуск шаблону з новими даними

```javascript
// У браузері (frontend)
fetch('/generate', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(newData)
})
.then(response => response.text())
.then(html => {
  // Відкрити HTML у новій вкладці
  const blob = new Blob([html], { type: 'text/html' });
  const url = URL.createObjectURL(blob);
  window.open(url, '_blank');
});
```

---

## 🚀 Оптимізація

### Статичні файли (кеш)

```javascript
app.use(express.static(path.join(__dirname, 'public'), {
  maxAge: '1d' // Кешувати на 1 день
}));
```

### Розмір HTML

- Дефолтний HTML: ~79 KB
- З zipping: ~15 KB
- Завантажується за < 1 сек

### Зображення

- JPG для desktop (оригінальна якість)
- WebP для сучасних браузерів (меньше розміру)
- Mobile версії (меньше зображення для телефонів)

---

## 🐛 Логування

### Startup логи

```
✅ КОНСТРУКТОР ЛЕНДІНГІВ - ETАП 2 ТЕСТУВАННЯ

✓ ТЕСТ 1: Перевірка структури проекту
  ✅ Папка views існує
  ✅ Папка public існує
  ✅ Папка data існує
  ✅ Файл data/landing-data.json існує

✓ ТЕСТ 2: Перевірка JSON даних
  ✅ JSON успішно прочитаний (7479 bytes)
  ✅ Товарів: 5
  ✅ Переваг: 5

✓ ТЕСТ 3: Express та EJS
  ✅ Express v5.1.0
  ✅ EJS v3.1.10

✅ СЕРВЕР ЗАПУЩЕНИЙ НА ПОРТУ 6614
```

### Request логи

```
✓ GET /generate: Генерація HTML
  ✅ JSON прочитаний
  ✅ Рендеринг template.ejs
  ✅ HTML згенерований (79425 bytes)

✓ POST /generate: Генерація HTML з користувацькими даними
  ✅ Отримано дані
  ✅ Рендеринг template.ejs
  ✅ HTML згенерований (75892 bytes)
  ✅ Товарів у результаті: 5
```

---

## 📈 Можливості розширення

### 1. Додати нові типи даних
```json
"testimonials": [...],
"faq": [...],
"gallery": [...]
```

### 2. Розширити форму
- Додати inline edit для товарів
- Завантаження зображень
- Попередній перегляд на сторінці

### 3. Зберігання проектів
- Зберігати JSON в базу даних
- Історія змін
- Експорт HTML/ZIP

### 4. Мультиязичність
```json
"translations": {
  "uk": {...},
  "en": {...},
  "ru": {...}
}
```

### 5. Шаблони
- Кілька базових дизайнів
- Темні/світлі варіанти
- Кастомізація кольорів

---

## 🔐 Безпека

### Поточні заходи
- HTML екранування в EJS (автоматично)
- Content-Type заголовки
- CORS не включений (тільки локальний доступ)

### Рекомендації для production
- HTTPS
- Валідація JSON input
- Санітизація HTML
- Rate limiting

---

## 📚 Документація файлів

| Файл | Розмір | Опис |
|------|--------|------|
| server.js | 16 KB | Express сервер |
| template.ejs | 81 KB | EJS шаблон |
| landing-data.json | 7.5 KB | Дефолтні дані |
| main.css | 56 KB | Основні стилі |
| main.js | 25 KB | JavaScript |
| public/img | 2+ MB | Зображення |

---

## ✅ Тестові сценарії

### Сценарій 1: Дефолтна генерація
1. GET /generate
2. Отримати 79 KB HTML
3. Перевірити 5 переваг
4. Перевірити 5 товарів
5. ✅ PASS

### Сценарій 2: Custom JSON
1. Створити JSON з 3 товарами
2. POST /generate з новим JSON
3. Отримати HTML з 3 товарами
4. ✅ PASS

### Сценарій 3: API Integration
1. GET /api/data
2. Отримати повний JSON
3. Модифікувати дані
4. Надіслати POST /generate
5. ✅ PASS

---

## 🎓 Висновок

Конструктор повністю готовий до використання:
- ✅ Архітектура стабільна
- ✅ Код чистий та документований
- ✅ Тести пройдені
- ✅ Оптимізація завершена

**Можна використовувати для генерації лендінгів!**
