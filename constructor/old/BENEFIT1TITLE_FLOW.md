# 🔍 ТОЧНИЙ FLOW: `{{benefit1Title}}`

## Від збереження до відображення на лендінгу

---

## 📋 Зміст
1. [Початок: Де зберігається значення](#1-початок-де-зберігається-значення)
2. [Користувач натискає ПЕРЕГЛЯД](#2-користувач-натискає-перегляд)
3. [Клієнтська функція previewSite()](#3-клієнтська-функція-previewsite)
4. [API endpoint POST /generate](#4-api-endpoint-post-generate)
5. [Функція generateHTML()](#5-функція-generatehtml)
6. [Читання шаблону template.ejs](#6-читання-шаблону-templateejs)
7. [Заміна плейсхолдера](#7-заміна-плейсхолдера)
8. [Повернення готового HTML](#8-повернення-готового-html)
9. [Відправка браузеру](#9-відправка-браузеру)
10. [Відображення користувачу](#10-відображення-користувачу)

---

## 1. Початок: Де зберігається значення

### 📄 Файл: `data/user-config.json`
**Рядок: 48-55**

```json
"benefits": [
  {
    "id": 1,
    "enabled": true,
    "title": "Безкоштовна доставка",    ← ЦЕ ЗНАЧЕННЯ
    "description": "Ми пропонуємо Максимально Безпечну покупку!"
  },
  {
    "id": 2,
    "enabled": true,
    "title": "Накладний платіж",
    "description": "Сплачуйте на пошті після огляду..."
  },
  ...
]
```

**Структура даних:**
- Тип: Масив об'єктів
- Поле: `benefits[0].title`
- Значення: `"Безкоштовна доставка"`

**Як потрапило сюди:**
- Користувач заповнив форму `sections/benefits.html`
- Натиснув "💾 Зберегти"
- Викликалась функція `saveFormToServer()` з `js/form.js`
- Відправився POST запит на `/api/save-config`
- Сервер записав у файл `data/user-config.json`

---

## 2. Користувач натискає ПЕРЕГЛЯД

### 📄 Файл: `form.html`
**Рядок: 131-133**

```html
<button type="button" class="button button-secondary" onclick="previewSite()">
  👁️ ПЕРЕГЛЯД
</button>
```

**Що відбувається:**
- Користувач натискає кнопку
- Викликається JavaScript функція `previewSite()`
- Функція знаходиться у файлі `js/form.js`

---

## 3. Клієнтська функція previewSite()

### 📄 Файл: `js/form.js`
**Рядок: 1984-2038**

```javascript
function previewSite() {
  // Use POST instead of GET to avoid URL length limits
  console.log('🔍 Preview button clicked');

  try {
    // ШАГ 1: Збирає всі дані з форми
    console.log('📊 Step 1: Getting form params...');
    const paramsString = getFormParams();

    // paramsString містить всі поля форми, включаючи:
    // benefits: JSON.stringify([
    //   { id: 1, title: "Безкоштовна доставка", ... },
    //   ...
    // ])

    if (!paramsString) {
      throw new Error('getFormParams() повернув порожній результат');
    }

    console.log('✅ Step 1 OK: Form params length:', paramsString.length);

    // ШАГ 2: Парсить параметри
    console.log('📊 Step 2: Parsing URLSearchParams...');
    const params = new URLSearchParams(paramsString);
    const paramsCount = Array.from(params).length;
    console.log('✅ Step 2 OK: Total params:', paramsCount);

    if (paramsCount === 0) {
      throw new Error('Немає параметрів для відправки');
    }

    // ШАГ 3: Створює невидиму форму
    console.log('📊 Step 3: Creating form...');
    const form = document.createElement('form');
    form.method = 'POST';
    form.action = '/generate';  // ← API ENDPOINT
    form.target = '_blank';     // Відкрити в новому вікні
    form.style.display = 'none';
    console.log('✅ Step 3 OK: Form created');

    // ШАГ 4: Додає всі параметри як hidden inputs
    console.log('📊 Step 4: Adding hidden inputs...');
    let inputCount = 0;
    for (const [key, value] of params) {
      const input = document.createElement('input');
      input.type = 'hidden';
      input.name = key;
      input.value = value;
      form.appendChild(input);
      inputCount++;

      // Одне з цих полів:
      // <input name="benefits" value='[{"id":1,"title":"Безкоштовна доставка",...}]'>
    }
    console.log('✅ Step 4 OK: Added', inputCount, 'inputs');

    // ШАГ 5: Відправляє форму
    console.log('📊 Step 5: Submitting form...');
    document.body.appendChild(form);
    form.submit();  // ← POST /generate
    document.body.removeChild(form);
    console.log('✅ Step 5 OK: Form submitted successfully!');

  } catch (error) {
    console.error('❌ ERROR:', error);
    console.error('❌ Stack:', error.stack);
    alert('ПОМИЛКА:\n\n' + error.message + '\n\nДивись консоль (F12) для деталей');
  }
}
```

**Функція викликає:**
- `getFormParams()` - збирає дані з форми (рядок 1735-1980)

**Що відправляється:**
```
POST /generate
Content-Type: application/x-www-form-urlencoded

benefits=[{"id":1,"enabled":true,"title":"Безкоштовна доставка","description":"Ми пропонуємо Максимально Безпечну покупку!"},...]
heroTitle=...
faqLabel=...
...всі інші поля...
```

---

## 4. API endpoint POST /generate

### 📄 Файл: `server.js`
**Рядок: 1557-1578**

```javascript
app.post('/generate', (req, res) => {
  try {
    // ШАГ 1: Отримує дані з форми
    const customData = req.body || {};

    // customData тепер містить:
    // {
    //   benefits: '[{"id":1,"title":"Безкоштовна доставка",...}]',  // ← STRING!
    //   heroTitle: "...",
    //   ...
    // }

    console.log('Отримані дані з форми:', Object.keys(customData).length, 'полів');

    // ШАГ 2: Читає fallback конфіг
    const dataPath = path.join(__dirname, 'data', 'user-config.json');
    const defaultData = JSON.parse(fs.readFileSync(dataPath, 'utf8'));

    // defaultData тепер містить:
    // {
    //   benefits: [
    //     { id: 1, title: "Безкоштовна доставка", ... }  // ← МАСИВ!
    //   ],
    //   ...
    // }

    // ШАГ 3: Об'єднує дані (пріоритет: форма > конфіг)
    const mergedData = { ...defaultData, ...customData };

    // mergedData.benefits тепер:
    // - Якщо прийшло з форми (customData.benefits) → STRING або МАСИВ
    // - Якщо не прийшло → береться з defaultData.benefits (МАСИВ)

    console.log(`\n🎨 ГЕНЕРУВАННЯ З CUSTOM ДАНИМИ...`);

    // ШАГ 4: Викликає функцію генерації HTML
    const html = generateHTML(mergedData);
    // ↑ ВИКЛИКАЄТЬСЯ ФУНКЦІЯ generateHTML()
    // Передається: mergedData (об'єкт з benefits)
    // Повертається: готовий HTML (string)

    console.log(`✅ Сайт успішно згенерований з custom даними`);
    console.log(`📏 Розмір: ${(html.length / 1024).toFixed(2)} KB\n`);

    // ШАГ 5: Відправляє HTML браузеру
    res.setHeader('Content-Type', 'text/html; charset=utf-8');
    res.send(html);
    // ↑ HTML ВІДПРАВЛЯЄТЬСЯ БРАУЗЕРУ

  } catch (err) {
    console.error('❌ Помилка:', err.message);
    res.status(500).json({ error: err.message });
  }
});
```

**HTTP Response:**
```
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!DOCTYPE html>
<html>
...готовий HTML з заміненими плейсхолдерами...
<b>Безкоштовна доставка</b>
...
</html>
```

**Функція викликає:**
- `generateHTML(mergedData)` - головна функція генерації (рядок 251)

---

## 5. Функція generateHTML()

### 📄 Файл: `server.js`
**Рядок: 251-639**

```javascript
function generateHTML(dataObj, options = {}) {
  // dataObj = mergedData з POST /generate
  // dataObj.benefits = [
  //   { id: 1, title: "Безкоштовна доставка", ... },
  //   ...
  // ]

  try {
    // ШАГ 1: Читає шаблон
    const templatePath = path.join(__dirname, 'views', 'template.ejs');
    let html = fs.readFileSync(templatePath, 'utf8');
    // ↑ ЧИТАЄ ФАЙЛ views/template.ejs

    // html тепер містить ВЕСЬ HTML шаблон з {{placeholder}}
    // включаючи: <b>{{benefit1Title}}</b>

    console.log(`📄 Шаблон прочитано: ${html.length} байт`);

    // ШАГ 2: Замінює основні поля
    // (рядки 256-301: headerText, heroTitle, heroPrice, etc.)

    const finalHeaderText = (options.headerText && options.headerText.trim())
      ? options.headerText
      : (dataObj.headerText || '');
    html = html.replace('{{headerText}}', finalHeaderText);

    // ...сотні таких замін...

    // ШАГ 3: Викликає модульні заміни
    // (рядок 303-305)
    const applyAllReplacements = require('./server/replacements');
    html = applyAllReplacements(html, options, dataObj);
    // ↑ ВИКЛИКАЄ server/replacements/index.js
    // (tabs, faq, howto, request, seo - 40 полів)

    // ШАГ 4: Видаляє вимкнені секції
    // (рядки 307-551: таймер, стоки, продукти, etc.)

    if (options.enableTimer !== 'on' && options.enableTimer !== true) {
      html = html.replace(/\s*<!--\s*timer\s*-->[\s\S]*?<!--\s*\/timer\s*-->\s*/g, '');
    }

    // ...інші видалення...

    // ШАГ 5: Замінює BENEFITS ← ТУТ НАШ ПЛЕЙСХОЛДЕР!
    // (рядки 624-631)

    // Замінити переваги (простій текстовий заміни плейсхолдерів)
    if (options.benefits && Array.isArray(options.benefits)) {
      options.benefits.forEach((benefit) => {
        const num = benefit.id;
        html = html.replace(`{{benefit${num}Title}}`, benefit.title);
        // ↑ ОСЬ ТУТ ВІДБУВАЄТЬСЯ ЗАМІНА!
        // `{{benefit1Title}}` → "Безкоштовна доставка"

        html = html.replace(`{{benefit${num}Description}}`, benefit.description);
      });
    }

    // ПІСЛЯ ЗАМІНИ:
    // html містить: <b>Безкоштовна доставка</b>
    // замість: <b>{{benefit1Title}}</b>

    console.log(`✅ HTML успішно згенерований (${html.length} байт)`);

    // ШАГ 6: Повертає готовий HTML
    return html;
    // ↑ ПОВЕРТАЄТЬСЯ В POST /generate (рядок 1567)

  } catch (err) {
    console.error('❌ Помилка при генеруванні HTML:', err.message);
    throw err;
  }
}
```

**Деталі ЗАМІНИ (рядок 624-631):**

```javascript
// options.benefits = [
//   { id: 1, title: "Безкоштовна доставка", description: "..." },
//   { id: 2, title: "Накладний платіж", description: "..." },
//   { id: 3, title: "Точно підберемо...", description: "..." },
//   { id: 4, title: "Гарантія якості", description: "..." }
// ]

// ЦИКЛ:
// Ітерація 1:
//   num = 1
//   benefit.title = "Безкоштовна доставка"
//   html.replace('{{benefit1Title}}', "Безкоштовна доставка")
//   html.replace('{{benefit1Description}}', "Ми пропонуємо...")

// Ітерація 2:
//   num = 2
//   benefit.title = "Накладний платіж"
//   html.replace('{{benefit2Title}}', "Накладний платіж")
//   html.replace('{{benefit2Description}}', "Сплачуйте на пошті...")

// ...і так далі для всіх benefits
```

**Що використовує:**
- `fs.readFileSync()` - Node.js функція читання файлу
- `String.replace()` - JavaScript метод заміни в строці
- `require('./server/replacements')` - підключає модуль замін

---

## 6. Читання шаблону template.ejs

### 📄 Файл: `server.js`
**Рядок: 253-254**

```javascript
const templatePath = path.join(__dirname, 'views', 'template.ejs');
let html = fs.readFileSync(templatePath, 'utf8');
```

**Що відбувається:**
1. `path.join(__dirname, 'views', 'template.ejs')` → `C:\ComoYo\Suka\constructor\views\template.ejs`
2. `fs.readFileSync()` читає файл як UTF-8 текст
3. Весь вміст файлу потрапляє в змінну `html`

---

### 📄 Файл: `views/template.ejs`
**Рядок: 170-190**

```html
<div class="section plus">
    <div class="container">
        <ul class="plus-list">
            <li class="plus-list_point">
                <img class="plus-list_icon" src="icons/plus/plus-1.svg" alt="img">
                <span class="plus-list_descr text-l">
                    <b>{{benefit1Title}}</b> <br>{{benefit1Description}}
                </span>
            </li>
            <li class="plus-list_point">
                <img class="plus-list_icon" src="icons/plus/plus-2.svg" alt="img">
                <span class="plus-list_descr text-l">
                    <b>{{benefit2Title}}</b> <br>{{benefit2Description}}
                </span>
            </li>
            <li class="plus-list_point">
                <img class="plus-list_icon" src="icons/plus/plus-3.svg" alt="img">
                <span class="plus-list_descr text-l">
                    <b>{{benefit3Title}}</b> <br>{{benefit3Description}}
                </span>
            </li>
            <li class="plus-list_point">
                <img class="plus-list_icon" src="icons/plus/plus-4.svg" alt="img">
                <span class="plus-list_descr text-l">
                    <b>{{benefit4Title}}</b> <br>{{benefit4Description}}
                </span>
            </li>
        </ul>
    </div>
</div>
```

**Структура:**
- Секція з class="section plus"
- 4 елементи `<li>` для 4-х переваг
- Кожен має `{{benefitNTitle}}` та `{{benefitNDescription}}`

**Після читання:**
Змінна `html` в `server.js` тепер містить цей HTML з плейсхолдерами.

---

## 7. Заміна плейсхолдера

### 📄 Файл: `server.js`
**Рядок: 624-631**

```javascript
// Замінити переваги (простій текстовий заміни плейсхолдерів)
if (options.benefits && Array.isArray(options.benefits)) {
  options.benefits.forEach((benefit) => {
    const num = benefit.id;  // num = 1

    // ДО ЗАМІНИ (змінна html):
    // <b>{{benefit1Title}}</b>

    html = html.replace(`{{benefit${num}Title}}`, benefit.title);
    // String.replace(шукати, замінити_на)
    // String.replace('{{benefit1Title}}', "Безкоштовна доставка")

    // ПІСЛЯ ЗАМІНИ (змінна html):
    // <b>Безкоштовна доставка</b>

    html = html.replace(`{{benefit${num}Description}}`, benefit.description);
    // String.replace('{{benefit1Description}}', "Ми пропонуємо...")
  });
}
```

**Детальний процес:**

```javascript
// BEFORE:
let html = `
  <span class="plus-list_descr text-l">
    <b>{{benefit1Title}}</b> <br>{{benefit1Description}}
  </span>
`;

// EXECUTE:
const benefit = { id: 1, title: "Безкоштовна доставка", description: "Ми пропонуємо..." };
const num = benefit.id;  // 1
html = html.replace(`{{benefit1Title}}`, benefit.title);
// ↑ Шукає в строці `html` текст "{{benefit1Title}}"
// ↑ Замінює його на "Безкоштовна доставка"

// AFTER:
html = `
  <span class="plus-list_descr text-l">
    <b>Безкоштовна доставка</b> <br>{{benefit1Description}}
  </span>
`;

// EXECUTE:
html = html.replace(`{{benefit1Description}}`, benefit.description);

// FINAL:
html = `
  <span class="plus-list_descr text-l">
    <b>Безкоштовна доставка</b> <br>Ми пропонуємо Максимально Безпечну покупку!
  </span>
`;
```

**Використовує:**
- `String.replace(search, replace)` - JavaScript метод
- **НЕ** regex (`/pattern/g`), а просту строку
- **НЕ** глобальна заміна - тільки перше входження

**Чому працює без глобальної заміни?**
Кожен плейсхолдер (`{{benefit1Title}}`) зустрічається в шаблоні **тільки ОДИН раз**, тому достатньо простої заміни.

---

## 8. Повернення готового HTML

### 📄 Файл: `server.js`
**Рядок: 633-634**

```javascript
console.log(`✅ HTML успішно згенерований (${html.length} байт)`);
return html;
```

**Що повертається:**
Функція `generateHTML()` повертає змінну `html` - це **повний HTML документ** (string) з усіма заміненими плейсхолдерами.

**Куди повертається:**
Назад в `POST /generate` endpoint (рядок 1567):

### 📄 Файл: `server.js`
**Рядок: 1567**

```javascript
const html = generateHTML(mergedData);
// ↑ html тепер містить готовий HTML
```

---

## 9. Відправка браузеру

### 📄 Файл: `server.js`
**Рядок: 1569-1573**

```javascript
console.log(`✅ Сайт успішно згенерований з custom даними`);
console.log(`📏 Розмір: ${(html.length / 1024).toFixed(2)} KB\n`);

res.setHeader('Content-Type', 'text/html; charset=utf-8');
res.send(html);
```

**Що відбувається:**
1. `res.setHeader()` - встановлює HTTP заголовок `Content-Type: text/html`
2. `res.send(html)` - відправляє HTML браузеру

**HTTP Response:**
```
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8
Content-Length: 245678

<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>Худі жіночі на флісі - РОЗПРОДАЖ</title>
  ...
</head>
<body>
  ...
  <div class="section plus">
      <div class="container">
          <ul class="plus-list">
              <li class="plus-list_point">
                  <img class="plus-list_icon" src="icons/plus/plus-1.svg" alt="img">
                  <span class="plus-list_descr text-l">
                      <b>Безкоштовна доставка</b> <br>Ми пропонуємо Максимально Безпечну покупку!
                  </span>
              </li>
              ...
          </ul>
      </div>
  </div>
  ...
</body>
</html>
```

---

## 10. Відображення користувачу

### 🌐 Браузер

**Що відбувається:**
1. Браузер отримує HTML відповідь від сервера
2. Парсить HTML
3. Рендерить сторінку
4. Завантажує додаткові ресурси:
   - CSS: `public/css/style.css`
   - JS: `public/js/main.js`
   - Зображення: `public/img/plus/plus-1.svg`

**Що бачить користувач:**

```
┌─────────────────────────────────────────┐
│                                         │
│  🎯 Безкоштовна доставка               │
│  Ми пропонуємо Максимально Безпечну    │
│  покупку!                               │
│                                         │
│  💰 Накладний платіж                   │
│  Сплачуйте на пошті після огляду...    │
│                                         │
│  📏 Точно підберемо потрібний Вам      │
│  розмір                                 │
│                                         │
│  ✅ Гарантія якості                    │
│  100% якість та задоволеність          │
│                                         │
└─────────────────────────────────────────┘
```

**DOM елемент:**
```html
<span class="plus-list_descr text-l">
  <b>Безкоштовна доставка</b>
  <br>
  Ми пропонуємо Максимально Безпечну покупку!
</span>
```

---

## 📊 ПОВНА СХЕМА FLOW

```
┌─────────────────────────────────────────────────────────────┐
│ 1. ФАЙЛ: data/user-config.json (рядок 53)                  │
│    "title": "Безкоштовна доставка"                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. КНОПКА: form.html (рядок 131)                           │
│    <button onclick="previewSite()">ПЕРЕГЛЯД</button>       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. ФУНКЦІЯ: js/form.js → previewSite() (рядок 1984)        │
│    - Збирає дані: getFormParams()                          │
│    - Створює форму: document.createElement('form')         │
│    - Відправляє: form.submit() → POST /generate           │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓ HTTP POST
┌─────────────────────────────────────────────────────────────┐
│ 4. API: server.js → POST /generate (рядок 1557)            │
│    - Отримує: req.body (дані з форми)                     │
│    - Читає: fs.readFileSync('data/user-config.json')      │
│    - Об'єднує: mergedData = {...defaultData, ...customData}│
│    - Викликає: generateHTML(mergedData)                    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. ФУНКЦІЯ: server.js → generateHTML() (рядок 251)         │
│    - Читає шаблон: fs.readFileSync('views/template.ejs')  │
│    - Замінює поля: html.replace()                          │
│    - Повертає: готовий HTML                                 │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5a. ЧИТАННЯ: server.js (рядок 254)                         │
│     let html = fs.readFileSync('views/template.ejs')       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5b. ШАБЛОН: views/template.ejs (рядок 175)                 │
│     <b>{{benefit1Title}}</b>                                │
│     ↑ цей текст тепер у змінній html                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5c. ЗАМІНА: server.js (рядок 628)                          │
│     html = html.replace('{{benefit1Title}}',               │
│                         "Безкоштовна доставка")             │
│     ↑ String.replace() метод JavaScript                    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 5d. РЕЗУЛЬТАТ: змінна html                                  │
│     <b>Безкоштовна доставка</b>                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. ПОВЕРНЕННЯ: server.js (рядок 634)                       │
│    return html;  → повертає в POST /generate               │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. ВІДПРАВКА: server.js (рядок 1573)                       │
│    res.send(html);  → HTTP Response                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓ HTTP Response
┌─────────────────────────────────────────────────────────────┐
│ 8. БРАУЗЕР: Отримує HTML                                    │
│    - Парсить HTML                                           │
│    - Рендерить сторінку                                     │
│    - Завантажує CSS, JS, зображення                        │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│ 9. КОРИСТУВАЧ БАЧИТЬ:                                       │
│    🎯 Безкоштовна доставка                                 │
│    Ми пропонуємо Максимально Безпечну покупку!             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 КОРОТКИЙ ПІДСУМОК

### Файли по порядку:

1. **`data/user-config.json`** (рядок 53) - зберігає значення
2. **`form.html`** (рядок 131) - кнопка ПЕРЕГЛЯД
3. **`js/form.js`** (рядок 1984) - функція `previewSite()`
4. **`server.js`** (рядок 1557) - API endpoint `POST /generate`
5. **`server.js`** (рядок 251) - функція `generateHTML()`
6. **`server.js`** (рядок 254) - читання `views/template.ejs`
7. **`views/template.ejs`** (рядок 175) - плейсхолдер `{{benefit1Title}}`
8. **`server.js`** (рядок 628) - заміна `html.replace()`
9. **`server.js`** (рядок 634) - повернення HTML
10. **`server.js`** (рядок 1573) - відправка `res.send(html)`

### Функції по порядку:

1. `previewSite()` - збирає дані, відправляє форму
2. `getFormParams()` - збирає параметри з форми
3. `app.post('/generate', ...)` - Express.js route handler
4. `fs.readFileSync()` - читає `user-config.json`
5. `generateHTML(mergedData)` - головна функція генерації
6. `fs.readFileSync()` - читає `template.ejs`
7. `String.replace()` - замінює плейсхолдер
8. `res.send()` - відправляє HTML

### API endpoints:

- **POST /generate** - генерує HTML з даними форми

### Методи JavaScript:

- `document.createElement()` - створює елемент
- `form.submit()` - відправляє форму
- `fs.readFileSync()` - читає файл (Node.js)
- `JSON.parse()` - парсить JSON
- `String.replace()` - замінює текст
- `res.send()` - відправляє відповідь (Express.js)

---

**Створено:** 2025-11-17
**Параметр:** `{{benefit1Title}}`
**Значення:** "Безкоштовна доставка"
**Проект:** Landing Page Constructor
