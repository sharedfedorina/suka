# 🎨 Конструктор Лендінгів - Посібник запуску

## ✅ Статус: ГОТОВО ДО ВИКОРИСТАННЯ

Ваш конструктор лендінгів повністю розроблений і тестований.

---

## 🚀 Запуск

### Варіант 1: Автоматичний запуск (рекомендується)
Натисніть `start-browser.bat` - сервер запуститься і браузер автоматично відкриється.

### Варіант 2: Запуск сервера
1. Запустіть `start.bat`
2. Відкрийте браузер: http://localhost:6614/

### Варіант 3: Командний рядок
```bash
cd constructor
npm start
```

Сервер буде доступний за адресою: **http://localhost:6614/**

---

## 📋 Що вдалося реалізувати

### ✓ Етап 1: Підготовка даних
- ✅ Видобули дані з HTML (текст, ціни, фото, переваги)
- ✅ Створили структуру JSON (landing-data.json)
- ✅ Додали 5 товарів, 5 переваг, всі метаданні

### ✓ Етап 2: Node.js сервер
- ✅ Встановили Express.js та EJS
- ✅ Налаштували порт 6614
- ✅ Створили батники для запуску (start.bat, start-browser.bat)

### ✓ Етап 3: EJS шаблон
- ✅ Скопіювали повну структуру HTML (1530 рядків)
- ✅ Замінили всі жорсткі значення на EJS змінні
- ✅ Налаштували всі секції: hero, advantages, products, тощо

### ✓ Етап 4: Динамічні цикли
- ✅ Додали forEach цикл для переваг (5 елементів)
- ✅ Додали forEach цикл для товарів (5 елементів)
- ✅ Розробили POST /generate endpoint для користувацьких даних
- ✅ Створили красивий admin інтерфейс у браузері

---

## 🎯 Як це використовувати

### Варіант 1: Дефолтні дані
1. Откройте http://localhost:6614/
2. Натисніть: "Генерувати з дефолтними даними"
3. Буде згенерований лендінг у новій вкладці браузера

### Варіант 2: Власні дані
1. Откройте http://localhost:6614/
2. В текстовому полі редагуйте JSON дані
3. Змініть текст, ціни, назви товарів, тощо
4. Натисніть: "Генерувати з JSON"
5. Отримаєте готовий лендінг з вашими даними

### Варіант 3: API запит (для програм)
```bash
# Отримати дефолтні дані (JSON)
curl http://localhost:6614/api/data

# Отримати статус сервера
curl http://localhost:6614/api/status

# Генерувати HTML з дефолтними даними
curl http://localhost:6614/generate -o site.html

# Генерувати HTML з власними даними
curl -X POST http://localhost:6614/generate \
  -H "Content-Type: application/json" \
  -d @custom-data.json -o site.html
```

---

## 📊 Структура дефолтних даних

```json
{
  "metadata": {
    "title": "Професійні 3D принтери для виробництва | Printex Pro",
    "description": "...",
    "brand": "Printex Pro"
  },
  "hero": {
    "title": "Професійні 3D принтери",
    "price": "8 500",
    "currency": "грн",
    "count": "12",
    "count_unit": "принтерів за акційною ціною"
  },
  "advantages": [
    {
      "title": "Безкоштовна доставка",
      "description": "...",
      "icon": "icons/plus/plus-1.svg"
    },
    // ... ще 4 переваги
  ],
  "products": {
    "items": [
      {
        "name": "3D Принтер FDM Pro Max",
        "subtitle": "Точність: 0.1мм",
        "price_old": "13 200",
        "price_new": "8 500",
        "currency": "грн",
        "specs": {...}
      },
      // ... ще 4 товари
    ]
  }
}
```

**Загалом 5 товарів + 5 переваг + метаданні**

---

## 🔧 Маршрути (Routes)

| Метод | URL | Опис |
|-------|-----|------|
| GET | `/` | Головна сторінка з формою генерації |
| GET | `/api/data` | JSON дані (все) |
| GET | `/api/status` | Статус сервера |
| GET | `/generate` | Генерувати HTML з дефолтних даних |
| POST | `/generate` | Генерувати HTML з JSON body |

---

## 📁 Структура проекту

```
constructor/
├── server.js                    # Express сервер з усіма маршрутами
├── views/
│   └── template.ejs            # EJS шаблон (1530 рядків, з циклами)
├── data/
│   └── landing-data.json       # Дефолтні дані (5 товарів, 5 переваг)
├── public/
│   ├── css/
│   │   ├── main.css           # Основні стилі (56 KB)
│   │   └── plugins.css        # Стилі плагінів (26 KB)
│   ├── js/
│   │   ├── main.js            # JavaScript (слайдери, таймер)
│   │   └── plugins.js         # Swiper та інші плагіни
│   ├── fonts/                 # GoogleSans шрифти (WOFF)
│   ├── img/                   # Зображення (4+ MB)
│   ├── icons/                 # SVG іконки
│   └── video/                 # Промо видео
├── start.bat                   # Запуск сервера (командний рядок)
├── start-browser.bat          # Запуск сервера + браузер (рекомендується)
├── package.json               # Node.js залежності
└── ЗАПУСК.md                  # Цей файл
```

---

## ⚙️ Технічні характеристики

- **Порт:** 6614
- **Сервер:** Node.js + Express 5.1.0
- **Шаблонізатор:** EJS 3.1.10
- **CSS:** BEM методологія,respons дизайн
- **JavaScript:** Vanilla JS + Swiper.js
- **Шрифти:** GoogleSans WOFF
- **Компілювання:** Без build step, чисте HTML/CSS/JS

---

## 🧪 Тестування

При запуску сервер виводить логи з перевіркою:
- ✅ Структура проекту
- ✅ JSON дані (7.5 KB)
- ✅ Express та EJS версії
- ✅ Статичні файли (CSS, JS, шрифти)

Загенерований HTML:
- ✅ 5 переваг (динамічно з JSON)
- ✅ 5 товарів (динамічно з JSON)
- ✅ Усі CSS завантажуються
- ✅ Усі JS завантажуються
- ✅ Усі змінні підставлені правильно

---

## 💡 Приклади

### Редагування дефолтної ціни
У JSON змініть:
```json
"price_new": "8 500"  // На
"price_new": "6 999"
```

### Додавання нового товару
Скопіюйте один товар в масив `products.items` і змініть:
- `name`, `subtitle`
- `price_old`, `price_new`
- `id` (унікальний)
- `images`, `specs`

### Змінення бренду
Знайдіть у JSON:
```json
"brand": "Printex Pro"  // На
"brand": "Ваша компанія"
```

---

## ⚡ Швидкий старт (1 хвилина)

1. Запустіть `start-browser.bat`
2. Натисніть "Генерувати з дефолтними даними"
3. Готово! Ваш лендінг відкрився в браузері

---

## 🚫 Потрібна помощь?

### Сервер не стартує
- Перевірте, чи порт 6614 не зайнятий
- Спробуйте запустити заново

### HTML не генерується
- Перевірте JSON синтаксис у формі
- Перевірте браузерну консоль (F12) на помилки

### Стилі не завантажуються
- Очистіть кеш браузера (Ctrl+Shift+Delete)
- Перезавантажте сторінку (F5)

---

**Проект готов до використання! Успіхів!** 🎉
