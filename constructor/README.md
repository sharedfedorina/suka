# 🚀 КОНСТРУКТОР ЛЕНДІНГІВ

## Запуск сервера

Є **два способи** запустити сервер:

### 1️⃣ Простий запуск
Двічі клікніть на файл:
```
start.bat
```

Сервер запуститься в консолі. Вам потрібно буде вручну відкрити браузер та перейти на:
```
http://localhost:6614/
```

### 2️⃣ Запуск з автоматичним відкриттям браузера ⭐ (Рекомендується)
Двічі клікніть на файл:
```
start-browser.bat
```

Це автоматично:
- Запустить сервер
- Відкриє браузер на `http://localhost:6614/`
- Дозволить вам тестувати конструктор

---

## Напишіть команди вручну (якщо батники не працюють)

Відкрийте командний рядок (CMD) в папці `constructor/` і введіть:

```bash
npm start
```

Потім відкрийте браузер:
```
http://localhost:6614/
```

---

## Структура проекту

```
constructor/
├── server.js              # Основний сервер
├── package.json           # Залежності
├── start.bat              # Запуск сервера
├── start-browser.bat      # Запуск + браузер
├── data/
│   └── landing-data.json  # Дані для конструктора
├── public/
│   ├── css/              # Стилізація
│   ├── js/               # JavaScript функціональність
│   ├── fonts/            # Google Sans шрифти
│   ├── img/              # Зображення
│   ├── icons/            # SVG іконки
│   └── favicon/          # Favicon
└── views/
    └── (EJS шаблони - в розробці)
```

---

## Порти і адреси

- **Порт:** 6614
- **Адреса:** http://localhost:6614/
- **JSON дані:** http://localhost:6614/api/data
- **Статус сервера:** http://localhost:6614/api/status

---

## Для зупинки сервера

Натисніть в консолі:
```
CTRL+C
```

---

## Далі

Наступна фаза: розробка **EJS шаблону** для генерації HTML з JSON даних.

