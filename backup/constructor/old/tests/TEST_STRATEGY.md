# 🧪 СТРАТЕГІЯ АВТОМАТИЧНОГО ТЕСТУВАННЯ КОНСТРУКТОРА

## 📋 Огляд

Система автоматичного тестування для перевірки цілісності генерації лендінгу з конфігураційних файлів.

## 🎯 Цілі тестування

1. **Повнота даних** - всі плейсхолдери мають бути заповнені
2. **Цілісність ресурсів** - всі файли (картинки/відео) мають існувати
3. **Консистентність конфігурації** - поля в формі мають відповідати темплейту
4. **Збереження даних** - дані мають коректно зберігатися в user-config.json
5. **Завантаження даних** - дані мають коректно завантажуватися з обох джерел

## 🔍 Типи тестів

### 1. **Тест плейсхолдерів (Placeholder Test)**

**Що перевіряє:**
- Відсутність незаповнених `{{...}}` в згенерованому HTML
- Всі очікувані плейсхолдери мають відповідні значення в конфігурації

**Як працює:**
1. Генерує HTML через GET `/generate`
2. Шукає всі `{{...}}` патерни в HTML
3. Перевіряє які плейсхолдери очікуються в темплейті
4. Звіряє з наявними даними в user-config.json

**Критерії успіху:**
- ✅ Немає `{{...}}` в згенерованому HTML
- ✅ Всі поля з темплейту присутні в конфігурації

### 2. **Тест ресурсів (Asset Test)**

**Що перевіряє:**
- Існування всіх файлів картинок/відео вказаних в конфігурації
- Існування файлів посилання на які є в згенерованому HTML

**Як працює:**
1. Читає всі шляхи з user-config.json (heroImage, imageUrl, videoUrl, product*Images)
2. Витягує всі src/href з згенерованого HTML
3. Перевіряє fs.existsSync() для кожного файлу
4. Звіряє відносні шляхи з структурою public/

**Критерії успіху:**
- ✅ Всі файли з конфігурації існують на диску
- ✅ Всі посилання в HTML вказують на існуючі файли

### 3. **Тест форми (Form Field Test)**

**Що перевіряє:**
- Всі поля в form.html мають відповідні плейсхолдери в index.html
- Всі плейсхолдери в index.html мають поля в form.html

**Як працює:**
1. Парсить form.html і витягує всі name="..." атрибути
2. Парсить index.html (темплейт) і витягує всі {{...}} плейсхолдери
3. Перевіряє чи є відповідність: name="fieldName" → {{fieldName}}

**Критерії успіху:**
- ✅ Кожен name= в формі має відповідний {{}} в темплейті
- ✅ Кожен {{}} в темплейті (що не динамічний) має поле в формі

### 4. **Тест збереження (Save Test)**

**Що перевіряє:**
- POST /save-data коректно зберігає дані в user-config.json
- Структура JSON залишається валідною
- Масиви (benefits, product*Images) зберігаються коректно

**Як працює:**
1. Створює тестові дані (mock form data)
2. Надсилає POST /save-data
3. Читає user-config.json
4. Звіряє що збереглося з тим що відправили

**Критерії успіху:**
- ✅ Відповідь 200 OK
- ✅ Дані в user-config.json відповідають надісланим
- ✅ JSON валідний

### 5. **Тест завантаження (Load Test)**

**Що перевіряє:**
- GET /api/data віддає landing-data.json (оригінал)
- Форма при завантаженні читає user-config.json (збережене)
- Генерація використовує правильне джерело даних

**Як працює:**
1. Запитує GET /api/data → має бути landing-data.json
2. Перевіряє що GET /generate використовує user-config.json
3. Звіряє що дані з різних джерел не змішуються

**Критерії успіху:**
- ✅ /api/data віддає оригінальні дані (футболки Kopo)
- ✅ /generate використовує збережені дані (худі ComoYo)

## 🏗️ Структура тестів

```
constructor/
├── tests/
│   ├── TEST_STRATEGY.md          ← Цей файл
│   ├── test-runner.js             ← Головний runner
│   ├── placeholders.test.js       ← Тест плейсхолдерів
│   ├── assets.test.js             ← Тест ресурсів
│   ├── form-fields.test.js        ← Тест форми
│   ├── save-load.test.js          ← Тест збереження/завантаження
│   └── utils/
│       ├── extract-placeholders.js
│       ├── extract-assets.js
│       └── test-helpers.js
```

## 🚀 Запуск тестів

```bash
# Всі тести
npm test

# Окремий тест
node tests/placeholders.test.js

# З детальним виводом
npm test -- --verbose

# Тільки швидкі тести (без збереження)
npm test -- --fast
```

## 📊 Формат звіту

```
🧪 TESTING CONSTRUCTOR - 2024-01-11 15:30:45
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. PLACEHOLDERS TEST
   ✅ No unfilled placeholders (0 found)
   ✅ All template fields have config values (127/127)
   ⏱️  Duration: 0.43s

2. ASSETS TEST
   ❌ Missing files: 3
      • img/start/hero-1762710428290_m.webp
      • video/review-2.mp4
      • img/image/image_1_m.webp
   ⏱️  Duration: 0.12s

3. FORM FIELDS TEST
   ✅ All form fields match template (98 fields)
   ⚠️  Warnings: 2 orphan placeholders in template
   ⏱️  Duration: 0.08s

4. SAVE/LOAD TEST
   ✅ Data saves correctly
   ✅ Data loads from correct source
   ⏱️  Duration: 1.23s

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RESULT: 3/4 PASSED (75%)
❌ FAILED - Fix 3 missing asset files
```

## 🔄 CI/CD Integration

Тести можна інтегрувати в:
- Git pre-commit hook (перед комітом)
- GitHub Actions (при push)
- npm script (перед деплоєм)

```json
{
  "scripts": {
    "test": "node tests/test-runner.js",
    "precommit": "npm test",
    "predeploy": "npm test -- --strict"
  }
}
```

## 📝 Приклади використання

### Типовий робочий процес:

1. Редагую form.html, додаю нове поле `newField`
2. Додаю `{{newField}}` в index.html
3. Запускаю тести → FAILED (поле не в user-config)
4. Додаю значення в user-config.json
5. Запускаю тести → PASSED
6. Коміт

### При виявленні проблеми:

1. Тести показують "Missing: img/photo.jpg"
2. Шукаю де використовується цей шлях
3. Або виправляю шлях, або додаю файл
4. Перезапускаю тести

## 🎯 Метрики якості

- **Coverage:** % плейсхолдерів покритих тестами
- **Asset integrity:** % існуючих файлів
- **Form-Template sync:** % полів що співпадають
- **Save/Load reliability:** % успішних операцій

## 📌 Важливі зауваження

1. Тести НЕ змінюють дані - тільки читають
2. Save Test використовує окремий test-config.json
3. Asset Test перевіряє тільки локальні шляхи (не CDN)
4. Placeholder Test ігнорує коментарі в HTML

## 🔧 Налаштування

Файл `tests/config.json`:

```json
{
  "strictMode": false,          // Fail on warnings
  "checkExternalAssets": false, // Check CDN links
  "generateBeforeTest": true,   // Auto-generate HTML
  "verbose": false,             // Detailed output
  "timeout": 5000              // Test timeout (ms)
}
```
