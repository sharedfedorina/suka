# 🧪 СТРАТЕГІЯ ТЕСТУВАННЯ КОНСТРУКТОРА

## 1️⃣ ЛОГУВАННЯ НА КОЖНОМУ КРОЦІ

### Console Logs:
```javascript
// Перевірка читання JSON
console.log('✓ JSON loaded:', data.metadata.title);

// Перевірка передачи даних в шаблон
console.log('✓ Products count:', data.products.items.length);

// Перевірка генерації HTML
console.log('✓ HTML generated, length:', html.length);
```

---

## 2️⃣ ВАЛІДАЦІЯ JSON

Перед генерацією перевірити:
```javascript
function validateData(data) {
  const required = ['metadata', 'hero', 'products', 'advantages'];
  const missing = required.filter(field => !data[field]);
  
  if (missing.length > 0) {
    throw new Error('Missing fields: ' + missing.join(', '));
  }
  
  console.log('✓ JSON validation passed');
  return true;
}
```

---

## 3️⃣ ПЕРЕВІРКА ГЕНЕРОВАНОГО HTML

### На кожен крок:

**Крок 1: Прочитан JSON?**
```
✓ landing-data.json открыт
✓ 5 товарів
✓ 5 переваг
```

**Крок 2: Шаблон рендериться?**
```
✓ template.ejs compile
✓ Variables injected
✓ HTML generated (81K)
```

**Крок 3: HTML містить усі дані?**
```
grep "Професійні 3D принтери" output.html
grep "8 500" output.html
grep "FDM Pro Max" output.html
grep "<img src=" output.html | wc -l
```

**Крок 4: HTML валідний?**
```
✓ Всі <div> закриті
✓ Всі <img> мають src
✓ Всі <a> мають href
✓ CSS файли підключені
✓ JS файли підключені
```

---

## 4️⃣ ПЕРЕВІРКА В БРАУЗЕРІ

### Що перевіряти:
1. **Сторінка завантажується** без помилок
2. **CSS стилізація** працює (кольори, шрифти, макет)
3. **JavaScript** працює:
   - Слайдери
   - Таймер
   - Модальні вікна
   - Кнопки
4. **Адаптивність** (мобільний вигляд)
5. **Зображення** завантажуються

---

## 5️⃣ АВТОМАТИЧНІ ПЕРЕВІРКИ

### Скрипт для перевірки генерованого HTML:
```javascript
// checks.js
const fs = require('fs');

function checkHTML(filePath) {
  const html = fs.readFileSync(filePath, 'utf-8');
  
  const checks = {
    'Has title': html.includes('<title>'),
    'Has hero section': html.includes('start-title'),
    'Has products': html.includes('products-text_title'),
    'Has advantages': html.includes('plus-list'),
    'CSS loaded': html.includes('css/main.css'),
    'JS loaded': html.includes('js/main.js'),
    'All divs closed': (html.match(/<div/g) || []).length === 
                       (html.match(/<\/div/g) || []).length
  };
  
  console.log('\n✓ HTML CHECKS:');
  Object.entries(checks).forEach(([name, pass]) => {
    console.log(`  ${pass ? '✅' : '❌'} ${name}`);
  });
  
  return Object.values(checks).every(v => v === true);
}

module.exports = { checkHTML };
```

---

## 6️⃣ ПОРІВНЯННЯ З ОРИГІНАЛОМ

### Порівняти згенерований с оригіналом:
```bash
# Кількість строк
wc -l printer-landing/index.html
wc -l output/generated.html

# Кількість тегів
grep -o "</div>" printer-landing/index.html | wc -l
grep -o "</div>" output/generated.html | wc -l

# Структура CSS класів
grep 'class=' printer-landing/index.html | wc -l
grep 'class=' output/generated.html | wc -l
```

---

## 7️⃣ ТЕСТУВАННЯ НА КОЖНОМУ ЕТАПІ

### ЕТАП 2 (Node.js + Express):
```
✓ npm install виконаний без помилок
✓ server.js запускається (npm start)
✓ Сервер слухає на port 3000
✓ GET / повертає 200
✓ Console не має помилок
```

### ЕТАП 3 (EJS шаблон):
```
✓ template.ejs існує
✓ EJS синтаксис правильний (<%= %>)
✓ Шаблон рендериться з дефолтними даними
✓ Усі змінні підставляються
✓ Генерований HTML правильний
```

### ЕТАП 4 (Конструктор):
```
✓ POST /generate приймає JSON
✓ HTML генерується успішно
✓ Файл зберігається
✓ Можна завантажити файл
✓ Генерований сайт працює в браузері
```

---

## 🎯 КОНТРОЛЬНИЙ СПИСОК

На кожен крок робот перевіряє:

- [ ] **Синтаксис** - немає помилок в коді
- [ ] **Логування** - всі важливі кроки залогировані
- [ ] **JSON** - дані валідні та повні
- [ ] **HTML** - згенерований правильно
- [ ] **CSS** - стилізація працює
- [ ] **JS** - функціональність працює
- [ ] **Браузер** - все завантажується і відображається

---

## 💡 КОЖ ЭТАП - КОЖ ПЕРЕВІРКА

Я буду виводити на екран:
```
ЕТАП X: Назва етапу
───────────────────────────────────
✓ Перевірка 1
✓ Перевірка 2
❌ Перевірка 3 (FAILED: описание проблеми)

РЕЗУЛЬТАТ: 2/3 ✅
```

Якщо щось не так - будуть деталі помилки та рекомендації як виправити.

