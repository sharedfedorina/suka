# ПРОЕКТ: КОНСТРУКТОР ЛЕНДІНГІВ

## 1. ЩО ЦЕ ЗА ПРОЕКТ

**Бізнес-задача:**
Конструктор для швидкої генерації лендінгів товарів (жіночий одяг, трикотаж).
Валідація товарів перед запуском серії, запуск Meta Ads (FB/Instagram).

**Як працює:**
1. Форма конструктора → введення даних + upload фото/відео
2. Дані → `user-config.json`
3. Template з плейсхолдерами → простий string replace
4. Результат: згенерований `index.html`
5. Deploy: Netlify

**Шаблон:** Готовий HTML/CSS лендінг конкурента (скачаний, адаптований)

**ЦЕ ПРОСТИЙ ПРОЕКТ! 1-2 дні роботи максимум!**

---

## 2. АРХІТЕКТУРА (ЗАФІКСОВАНА - НЕ МІНЯТИ!)

### Структура проекту:

```
constructor/
├── form.html                    # Форма конструктора
├── server.js                    # Express сервер (НЕ ЧІПАТИ основну логіку!)
├── user-config.json            # КРИТИЧНИЙ! Збережені дані
│
├── /sections/                  # HTML секції форми (collapsible)
│   ├── basic.html              # Основні налаштування
│   ├── hero.html               # Hero секція
│   ├── benefits.html           # 5 переваг
│   ├── pluslogo.html           # Додаткове зображення
│   ├── video.html              # Відео + thumbnail
│   ├── products.html           # Товари (до 9 шт)
│   ├── sizechart.html          # Розмірна сітка
│   ├── tabs.html               # 3 вкладки переваг
│   ├── comments.html           # Відгуки + статистика
│   ├── reviews.html            # Відгуки клієнтів з фото
│   ├── faq.html                # FAQ (4 питання)
│   ├── howto.html              # Кроки покупки
│   ├── request.html            # Форма замовлення
│   ├── footer.html             # Footer
│   ├── seo.html                # SEO/Meta теги
│   └── salesdrive.html         # Sales Drive інтеграція
│
├── /modules/                   # HTML template модулі (для генерації)
│   ├── base.html               # Головний з {{MODULE_*}} плейсхолдерами
│   ├── basic.html              # <head>, Meta Pixel
│   ├── hero.html               # {{heroTitle}}, {{heroPrice}}...
│   ├── benefits.html           # {{benefit1Title}}...
│   ├── video.html              # {{videoUrl}}...
│   ├── products.html           # Динамічна генерація товарів
│   ├── sizechart.html          # {{sizeChartImage}}...
│   ├── tabs.html               # {{tab1Title}}...
│   ├── comments.html           # {{commentsLabel}}...
│   ├── faq.html                # {{faq1Question}}...
│   ├── howto.html              # {{howLabel}}...
│   ├── request.html            # {{requestInfoTitle}}...
│   └── footer.html             # {{footerCopyright}}...
│
├── /server/
│   └── /replacements/          # Модулі для replace плейсхолдерів
│       ├── index.js            # Координатор всіх модулів
│       ├── tabs.js             # Replace для tabs секції
│       ├── faq.js              # Replace для FAQ
│       ├── howto.js            # Replace для How To
│       ├── request.js          # Replace для форми
│       └── seo.js              # Replace для SEO
│
├── /lib/                       # Бібліотеки (МОДУЛЬНА АРХІТЕКТУРА!)
│   ├── constants.js            # Константи (шляхи, конфіги)
│   ├── config-manager.js       # Робота з user-config.json
│   ├── html-generator.js       # Генерація HTML з template
│   ├── media.js                # Upload фото/відео (Multer, Sharp)
│   └── logger.js               # Логування
│
├── /js/
│   ├── form.js                 # Frontend логіка конструктора
│   ├── main.js                 # Frontend лендінгу (jQuery, Swiper)
│   └── plugins.js              # jQuery плагіни
│
├── /scripts/
│   ├── regenerate-product-mobile.js    # Регенерація mobile WebP
│   └── regenerate-sizechart-mobile.js  # Регенерація size chart mobile
│
├── /netlify/
│   └── /functions/
│       └── submit-order.js     # Serverless функція (Sales Drive API)
│
├── /public/
│   └── /img/
│       ├── /hero/              # Головні зображення
│       ├── /products/          # Фото товарів
│       └── /info/              # Розмірна сітка
│
├── netlify.toml                # Netlify конфігурація
└── package.json                # Dependencies
```

---

## 3. 🔴 КРИТИЧНІ ПРАВИЛА (ПОРУШЕННЯ = КАТАСТРОФА!)

### ❌ ЗАБОРОНЕНО:

1. **Видаляти файли без git commit**
   - Особливо `user-config.json`!

2. **Перетирати файли, робити backup/restore/git reset**
   - Це знищує проект!

3. **Міняти структуру папок**
   - `/sections/`, `/modules/`, `/server/replacements/` - фіксовані!

4. **Об'єднувати модулі в один файл**
   - Модульна архітектура КРИТИЧНА для зменшення токенів!

5. **Ламати маппінг плейсхолдерів**
   - ID форми = config ключ = template плейсхолдер (ОДНАКОВО!)

6. **Ускладнювати рішення**
   - Простий replace "в лоб" - це ВСЕ що треба!

7. **Робити refactoring без дозволу**
   - Працює = НЕ ЧІПАЙ!

8. **Міняти більше 2 файлів без commit**
   - Після кожної зміни → commit!

9. **Читати великі файли цілком**
   - Використовуй модульну структуру!

10. **Вигадувати "розумні" рішення**
    - KISS principle! Простота > складність

---

### ✅ ОБОВ'ЯЗКОВО:

1. **ЗАВЖДИ git commit після КОЖНОЇ зміни:**
   ```bash
   git add .
   git commit -m "feat: що зробив"
   ```

2. **Перед будь-якими змінами:**
   ```bash
   git status
   ```

3. **Якщо не розумієш контекст - СПИТАЙ!**
   - Не вигадуй!

4. **Баг не фікситься за 3 спроби?**
   - ЗУПИНИСЬ, повідом людину

5. **Модульна архітектура**
   - Кожна функція = окремий файл

6. **Простота завжди!**
   - Тупий replace > складна логіка

---

## 4. МАППІНГ ПЛЕЙСХОЛДЕРІВ (КРИТИЧНО!)

**ЗОЛОТЕ ПРАВИЛО:**
```
ID поля форми = ключ у user-config.json = плейсхолдер у template
```

### Приклад:

**Форма (`/sections/hero.html`):**
```html
<input type="text" id="heroTitle" name="heroTitle" />
<input type="text" id="heroPrice" name="heroPrice" />
```

**Config (`user-config.json`):**
```json
{
  "heroTitle": "Худі жіночі на флісі",
  "heroPrice": "всього 610 грн"
}
```

**Template (`/modules/hero.html`):**
```html
<h1>{{heroTitle}}</h1>
<div class="price">{{heroPrice}}</div>
```

**ВСЕ ОДНАКОВО → простий replace!**

**🚨 НЕ МІНЯТИ НАЗВИ! НЕ ЛАМАТИ МАППІНГ!**

---

## 5. TECH STACK

### Backend:
- **Node.js** + **Express**
- **Multer** - upload файлів
- **Sharp** - обробка зображень (WebP, resize)
- **fs/path** - робота з файлами

### Frontend:
- **jQuery** - DOM маніпуляції
- **Swiper.js** - слайдери
- Vanilla JavaScript

### Deploy:
- **Netlify** - хостинг + serverless functions
- **Netlify Functions** - Sales Drive API (безпечно)

### Інтеграції:
- **Sales Drive CRM** - прийом замовлень
- **Meta Pixel** - Facebook відстеження

---

## 6. МОДУЛІ `/server/replacements/` (КРИТИЧНО!)

**Кожен модуль = окрема секція!**

### Структура модуля:

```javascript
// /server/replacements/hero.js
module.exports = function applyHeroReplacements(html, options, dataObj) {
  const heroTitle = options.heroTitle || dataObj.heroTitle || '';
  const heroPrice = options.heroPrice || dataObj.heroPrice || '';
  
  html = html.replace(/\{\{heroTitle\}\}/g, heroTitle);
  html = html.replace(/\{\{heroPrice\}\}/g, heroPrice);
  
  return html;
};
```

**ЧОМУ ЦЕ ВАЖЛИВО:**
- Якщо помилка в Hero → читаєш ТІЛЬКИ `hero.js` (100 рядків)
- А НЕ весь `server.js` (2000+ рядків)
- Економія токенів!

**🚨 НЕ ОБ'ЄДНУВАТИ МОДУЛІ!**

---

## 7. WORKFLOW ГЕНЕРАЦІЇ

### Крок 1: Збереження config
```
Форма → POST /api/save-config → user-config.json
```

### Крок 2: Генерація HTML
```
user-config.json
    ↓
lib/html-generator.js
    ↓
Читає /modules/base.html
    ↓
Замінює {{MODULE_HERO}} → /modules/hero.html
    ↓
Застосовує /server/replacements/hero.js
    ↓
Замінює {{heroTitle}} → "Худі жіночі"
    ↓
... (для всіх модулів)
    ↓
output/index.html
```

### Крок 3: Preview
```
GET /api/preview → повертає згенерований HTML
```

### Крок 4: Deploy
```
Netlify → upload згенерованого сайту
```

---

## 8. UPLOAD МЕДІА

### Hero Image:
```
POST /api/upload-hero-image
    ↓
Multer → /public/img/hero/hero-[timestamp].jpg
    ↓
Sharp → /public/img/hero/hero-[timestamp]_m.webp (mobile)
    ↓
Повертає шлях → user-config.json
```

### Product Images:
```
POST /api/upload-product-image/:productNumber
    ↓
/public/img/products/product-[timestamp].jpg
    ↓
Sharp → product-[timestamp]_m.webp
```

**ВАЖЛИВО:**
- Desktop: `.jpg`
- Mobile: `_m.webp`
- Автоматична генерація mobile версій!

---

## 9. SALES DRIVE ІНТЕГРАЦІЯ

### Безпечний workflow:

**❌ НЕПРАВИЛЬНО (небезпечно):**
```javascript
// Frontend - API ключ видно в браузері!
fetch('https://salesdrive.com/api', {
  headers: { 'X-Api-Key': 'secret_key' }
});
```

**✅ ПРАВИЛЬНО (безпечно):**
```javascript
// Frontend
fetch('/.netlify/functions/submit-order', {
  method: 'POST',
  body: JSON.stringify(orderData)
});

// Netlify Function
// API ключ в Environment Variables!
const apiKey = process.env.SALESDRIVE_API_KEY;
```

**Налаштування (Netlify Dashboard):**
```
SALESDRIVE_ENDPOINT=https://example.salesdrive.me/handler/
SALESDRIVE_API_KEY=xxxxx
SALESDRIVE_FUNNEL_ID=1
```

---

## 10. GIT WORKFLOW

### Після КОЖНОЇ логічної зміни:

```bash
git add .
git commit -m "feat: додав hero image upload"
```

### Приклади good commits:
```
feat: додав модуль hero.js
fix: виправив маппінг heroTitle
refactor: розбив server.js на модулі
style: форматування faq.html
docs: оновив CLAUDE.md
```

### 🚨 NO EXCEPTIONS!

---

## 11. КРИТИЧНІ ФАЙЛИ (READ-ONLY без дозволу)

```
user-config.json         # Дані користувача
package.json             # Dependencies
.env                     # Секрети (якщо є)
netlify.toml             # Netlify config
/modules/base.html       # Головний template
server.js                # Основна логіка (обережно!)
```

**Перед редагуванням → git commit!**

---

## 12. COLLAPSIBLE СЕКЦІЇ (ФОРМА)

**Використовуємо `<details>` теги:**

```html
<details class="form-section">
  <summary class="form-section-header">🎯 Hero секція</summary>
  <div class="form-section-content">
    <!-- Поля форми -->
  </div>
</details>
```

**По дефолту: згорнуті (collapsed)**

**Чому це важливо:**
- Зручність - не скролити 1000 полів
- Логічне групування
- Швидкий доступ до потрібної секції

---

## 13. ДИНАМІЧНА ГЕНЕРАЦІЯ (Товари/Слайдери)

**Єдиний виняток від простого replace!**

### Проблема:
- Різна кількість товарів (1-9)
- Різна кількість фото для кожного товару

### Рішення:

**Config:**
```json
{
  "enableProduct1": true,
  "product1Name": "Худі БЕЖ",
  "product1Images1": "/img/products/product-1.jpg",
  "product1Images2": "/img/products/product-2.jpg",
  "product1Images3": "/img/products/product-3.jpg"
}
```

**Генерація (html-generator.js):**
```javascript
// Для кожного увімкненого товару
if (config.enableProduct1) {
  const slides = [];
  for (let i = 1; i <= 9; i++) {
    const img = config[`product1Images${i}`];
    if (img) {
      slides.push(`
        <div class="swiper-slide">
          <img src="${img}" alt="Product 1" />
        </div>
      `);
    }
  }
  html = html.replace('{{PRODUCT1_SLIDES}}', slides.join(''));
}
```

**Це ЄДИНИЙ випадок складної логіки!**

---

## 14. META PIXEL

**Template (`/modules/basic.html`):**
```html
<script>
  fbq('init', '{{facebookPixelId}}');
  fbq('track', 'PageView');
</script>
```

**Config:**
```json
{
  "facebookPixelId": "3670672943154298"
}
```

**Replace → готово!**

---

## 15. ТИПОВІ ПОМИЛКИ (НЕ РОБИТИ!)

### ❌ Помилка #1: Все в один файл
```javascript
// ПОГАНО - 2000 рядків в server.js
function generateHTML() {
  // Вся логіка тут...
}
```

```javascript
// ДОБРЕ - модулі
const applyHeroReplacements = require('./replacements/hero');
const applyTabsReplacements = require('./replacements/tabs');
```

### ❌ Помилка #2: Ламати маппінг
```javascript
// ПОГАНО - різні назви!
<input id="hero_title" />           // hero_title
config.heroTitle                     // heroTitle
{{hero-title}}                       // hero-title
```

```javascript
// ДОБРЕ - всюди однаково!
<input id="heroTitle" />
config.heroTitle
{{heroTitle}}
```

### ❌ Помилка #3: Не робити commit
```bash
# ПОГАНО
[змінив 10 файлів]
[щось зламалось]
[не можу відкотити]
```

```bash
# ДОБРЕ
git commit -m "feat: додав hero"
[змінив 2 файли]
git commit -m "fix: виправив heroTitle"
[щось зламалось]
git reset --hard HEAD~1  # відкотив останній commit
```

### ❌ Помилка #4: Читати весь файл
```javascript
// ПОГАНО - завантажує весь server.js (токени!)
// Помилка в hero → читає 2000 рядків
```

```javascript
// ДОБРЕ - читає тільки hero.js (100 рядків)
// Помилка в hero → читає 100 рядків
```

---

## 16. DEBUGGING

### Якщо щось не працює:

**Крок 1: Перевір config**
```bash
GET /api/get-user-config
# Подивися чи є потрібний ключ
```

**Крок 2: Перевір template**
```bash
# Відкрий /modules/hero.html
# Чи є плейсхолдер {{heroTitle}}?
```

**Крок 3: Перевір replacement**
```bash
# Відкрий /server/replacements/hero.js
# Чи правильний replace?
```

**Крок 4: Перевір форму**
```bash
# Відкрий /sections/hero.html
# Чи правильний id="heroTitle"?
```

**Крок 5: Логи**
```bash
# Дивись constructor/server.log
```

---

## 17. NETLIFY DEPLOY

### Структура:

```
netlify.toml             # Конфігурація
/netlify/functions/      # Serverless функції
output/                  # Згенерований сайт (deploy)
```

### Environment Variables (Netlify Dashboard):
```
SALESDRIVE_ENDPOINT=...
SALESDRIVE_API_KEY=...
SALESDRIVE_FUNNEL_ID=1
```

### Deploy команда:
```bash
netlify deploy --prod
```

---

## 18. SCRIPTS

### Регенерація mobile WebP:
```bash
# Для товарів
node scripts/regenerate-product-mobile.js

# Для розмірної сітки
node scripts/regenerate-sizechart-mobile.js
```

**Коли використовувати:**
- Після додавання нових фото
- Якщо mobile версії відсутні

---

## 19. DEPENDENCIES (package.json)

### Основні:
```json
{
  "express": "^4.x",
  "multer": "^1.x",
  "sharp": "^0.x"
}
```

### Для встановлення:
```bash
npm install
```

---

## 20. ШВИДКИЙ СТАРТ

### Перший запуск:

1. **Встанови залежності:**
   ```bash
   npm install
   ```

2. **Запусти сервер:**
   ```bash
   node server.js
   ```

3. **Відкрий браузер:**
   ```
   http://localhost:3000/
   ```

4. **Заповни форму, збережи, preview!**

---

## 21. ЧОМУ ЦЕ ПРОСТИЙ ПРОЕКТ?

**Суть:**
- Форма → JSON → Template → Replace → HTML

**Що НЕ ТРЕБА:**
- ❌ База даних
- ❌ Аутентифікація
- ❌ Складна бізнес-логіка
- ❌ Реал-тайм оновлення
- ❌ API endpoints (окрім simple CRUD)

**Що ТРЕБА:**
- ✅ Форма (є)
- ✅ JSON config (є)
- ✅ Template (є)
- ✅ Simple replace (є)
- ✅ Модульна архітектура (є)

**1-2 дні роботи з правильним підходом!**

---

## 22. ВАЖЛИВІ НОТАТКИ

### Про контекст токени:
- Модульна архітектура КРИТИЧНА
- Якщо файл > 500 рядків → розбити на модулі
- Читати тільки потрібні модулі

### Про git:
- Commit після кожної зміни
- НЕ накопичувати багато змін
- Легше відкотити 1 commit, ніж 50

### Про простоту:
- Тупий replace працює
- Не вигадувай складних рішень
- KISS principle!

### Про безпеку:
- API ключі в Netlify Environment Variables
- НЕ в коді!
- НЕ в git!

---

## 23. КОНТАКТИ ДЛЯ ДОПОМОГИ

**Документація:**
- Express: https://expressjs.com/
- Multer: https://github.com/expressjs/multer
- Sharp: https://sharp.pixelplumbing.com/
- Netlify Functions: https://docs.netlify.com/functions/overview/

**Якщо застряг:**
1. Подивись логи (`constructor/server.log`)
2. Подивись config (`/api/get-user-config`)
3. Перевір git status
4. Спитай людину!

---

## 24. ФІНАЛЬНЕ НАГАДУВАННЯ

**🎯 МЕТА:** Зробити конструктор, який ПРАЦЮЄ!

**🚫 НЕ МЕТА:** Зробити "ідеальний код"

**✅ ПРІОРИТЕТИ:**
1. Працює
2. Просто
3. Модульно
4. З git commits

**❌ АНТИ-ПРІОРИТЕТИ:**
1. "Розумні" рішення
2. Refactoring без причини
3. Ускладнення
4. Зміни без commit

---

**УСПІХІВ! 🚀**
