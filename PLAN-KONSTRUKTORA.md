# 📐 ПЛАН РОЗРОБКИ КОНСТРУКТОРА ЛЕНДІНГІВ

## ✅ ЕТАП 1: ПІДГОТОВКА ДАНИХ (ЗАВЕРШЕНО)

### Що зроблено:
- ✅ Видобуто всі текстові дані з printer-landing
- ✅ Видобуто назви товарів, ціни, характеристики
- ✅ Видобуто шляхи до фото та відео
- ✅ Створена JSON структура (`landing-data.json`)
- ✅ JSON містить повні дані для конструктора

### Структура JSON:
```
landing-data.json
├── metadata (назва, заголовок, опис)
├── header (текст шапки)
├── hero (головна секція з заголовком, ціною)
├── action_block (блок дій)
├── advantages (5 переваг з descriptiоном)
├── promo (відео, заголовок)
├── products (5 товарів з усіма деталями)
├── media (картинки, відео)
├── colors (кольорова схема)
├── fonts (шрифти)
├── contacts (контакти)
└── seo (SEO дані)
```

---

## 📋 ЕТАП 2: НАЛАШТУВАННЯ NODE.JS ПРОЕКТУ

### План:
1. Створити папку `constructor/`
2. Ініціалізувати Node.js проект (`npm init`)
3. Встановити залежності:
   - **Express.js** - веб-фреймворк
   - **EJS** - template engine
   - **Sharp** - обробка зображень (потім)
   - **Nodemon** - для розробки

### Структура проекту:
```
constructor/
├── package.json
├── server.js              # Основний сервер
├── data/
│   └── landing-data.json  # Дані за замовчуванням
├── public/
│   ├── css/              # CSS для генерованого сайту
│   ├── js/               # JS для генерованого сайту
│   └── img/              # Зображення
├── views/
│   └── template.ejs      # EJS шаблон конструктора
├── routes/
│   ├── generate.js       # Маршрут для генерації
│   └── preview.js        # Маршрут для превью
└── controllers/
    └── generator.js      # Логіка генерації
```

---

## 🎨 ЕТАП 3: СТВОРЕННЯ EJS ШАБЛОНУ

### План:
1. Скопіювати структуру з printer-landing
2. Замінити жорсткі значення на EJS змінні:
   ```ejs
   <%= data.hero.title %>
   <%= data.advantages[i].description %>
   <% data.products.forEach(product => { %>
     <%= product.name %>
   <% }) %>
   ```
3. Розділити на компоненти:
   - `_header.ejs`
   - `_hero.ejs`
   - `_advantages.ejs`
   - `_products.ejs`
   - `_footer.ejs`

### Результат:
- Один шаблон, що підтримує будь-які дані з JSON

---

## 🛠️ ЕТАП 4: РОЗРОБКА КОНСТРУКТОРА

### 4.1 Базова генерація
- ✅ Прочитати JSON
- ✅ Передати дані в EJS шаблон
- ✅ Генерувати HTML
- ✅ Зберегти HTML файл

### 4.2 Функціональність:
```
GET  /                    # Сторінка конструктора (форма)
POST /generate            # Генерувати новий сайт
GET  /preview/:id         # Превью сайту
GET  /download/:id        # Завантажити сайт
GET  /api/data            # Отримати дефолтні дані
POST /api/data            # Зберегти нові дані
```

### 4.3 Тестування:
- Генерувати сайт з дефолтними даними
- Передати змінені дані
- Перевірити що генерується правильний HTML

---

## 📝 ЕТАП 5: ADMIN ФОРМА (ОПЦІОНАЛЬНО)

### План:
1. Форма для редагування JSON даних
2. Редагування текстів, цін, фото
3. Повний CRUD для товарів
4. Зберігання в базу даних або файли

---

## 🚀 ПОСЛІДОВНІСТЬ РОЗРОБКИ:

**СЬОГОДНІ:**
1. ✅ Підготувати дані (landing-data.json)

**ЗАВТРА:**
2. Налаштувати Node.js проект
3. Створити базовий Express сервер
4. Копіювати CSS/JS з printer-landing

**ПОТІМ:**
5. Розробити EJS шаблон
6. Розробити конструктор (генерацію HTML)
7. Тестування та оптимізація

---

## 💾 ФАЙЛИ ДЛЯ КОПІЮВАННЯ:

З `printer-landing/` в `constructor/public/`:
```
✓ css/main.css
✓ css/plugins.css
✓ js/main.js
✓ js/plugins.js
✓ fonts/
✓ icons/
✓ img/
```

---

## 🎯 ФІНАЛЬНА МЕТА:

```
Користувач:
1. Завантажує JSON з новими даними
2. Натискає "Generate"
3. Отримує готовий HTML сайт
4. Завантажує в любий хостинг
```

